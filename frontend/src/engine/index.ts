export { createInitialSimState } from "./init";
export { runStep } from "./step";
export { makeRng } from "./rng";
// Math & score functions are internal — use them via step.ts only
// Types are in @/lib/types, constants in @/lib/constants
